<div class="container">
  <div class="top-image">
    <div class="image">
      <p>スポーツを通じて生まれる新たな出会い</p>
    </div>
  </div>
  <div class="form-title">
    <h3><span>投稿一覧</span></h3>
  </div>
  <div class="posts">
    <div class="post-wrapper">
      <% @posts.each do |post| %>
        <div class="post">
          <%= link_to root_path, class: "post-link" do %>
            <div class="post-content">
              <div class="post-left">
                <div class="user-icon">
                  <% if post.user.image.attached? %>
                    <%= image_tag post.user.image, class: "user-icon about-post" %>
                  <% else %>
                    <%= image_tag  'default.png', class: "user-icon about-post"%>
                  <% end %>
                </div>
                <div class="user-info">
                  <div class="name">
                    <%= post.user.name %>
                  </div>
                  <div class="review">
                    レビューはありません
                  </div>
                </div>
              </div>
              <div class="post-right">
                <div class="title">
                  <%= post.title %>
                </div>
                <div class="content">
                  <div class="tags_list">
                    <% if post.tags.present? %>
                      <% post.tags.each do |tag| %>
                        <%= tag.name %>
                      <% end %>
                    <% end %>
                  </div>
                  <div class="sports">
                    <%= post.sports %>
                  </div>
                  <div class="datetime">
                    <%= post.datetime %>
                  </div>
                  <div class="region">
                    <%= post.region %>
                  </div>
                  <div class="cost">
                    <%= post.cost %>
                  </div>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
  <%= paginate @posts %>
</div>